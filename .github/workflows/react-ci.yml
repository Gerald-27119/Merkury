name: React CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches: [ master, develop ]
  merge_group:
    branches: [ master, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        working-directory: Merkury/venus
        run: npm install

      - name: Run tests
        working-directory: Merkury/venus
        run: npm test -- --watchAll=false
